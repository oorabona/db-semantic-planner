# Post-Compaction Recovery

**Compaction occurred at:** 2026-03-25T13:58:47+01:00
**Mode:** CONTINUOUS — resume and continue WITHOUT waiting for user

## Resume Instructions

1. MEMORY.md is auto-loaded in system prompt (no action needed)
   - If active.md exists, read: ~/.claude/projects/<sanitized-path>/memory/active.md

2. Resume at this exact state:
   - Story: FR-1
   - Stage: Stage 6 — /implement
   - Block: 1/3
   - Last checkpoint: Spec approved, context reset, starting Block 1

3. **CONTINUOUS MODE ACTIVE** — continue the workflow immediately:
   - Do NOT wait for user confirmation between stages
   - Do NOT skip quality gates: /review, /llm, tests, /finalize
   - STOP only for: [BLOCKED] or [PHASE_COMPLETE]

4. After reading memories, delete this file:
   `rm /mnt/wsl/shared/dev/db-semantic-planner/.claude-recovery.md`

## Project State Snapshot

### Git Changes
```
 6 files changed, 140 insertions(+), 14 deletions(-)
```

### Uncommitted Files
```
 M .claude-recovery.md
 M .workflow-state.json
 M packages/adapter-pgsql/src/compile-where.ts
 M packages/adapter-pgsql/src/compiler.ts
 M packages/adapter-pgsql/src/handlers/expression/custom.ts
 M packages/core/src/dx/index.ts
?? packages/adapter-pgsql/src/__tests__/case-when.test.ts
?? packages/core/src/dx/case-when-builder.ts
```

### TODO State
**In Progress:**
- 53:- [ ] 🟡 **GUI-026** [GUI] Dirty tab confirmation + session persistence + app close guard — Priority: P1

## CRITICAL RULES (non-negotiable)

- **NEVER use `git stash`** — use `git show HEAD:path` or `git diff HEAD` instead (data loss risk)
- **NO 'pre-existing' excuse** — you own ALL code. Fix ALL errors, don't filter before/after.
- **GOTCHAS first** — before ANY debugging, run: `grep -rni '<keyword>' ~/.claude/skills/*/GOTCHAS.md`
- **Deferred items use `[-]`** — only `[x]` lines get archived by /finalize
