---
layout: page
---

# Playground

Try db-semantic-planner's NQL query language directly in your browser. Queries are compiled to SQL using the compile-only adapter — no database connection needed.

<Playground />
