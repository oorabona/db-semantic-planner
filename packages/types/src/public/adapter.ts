/**
 * Public adapter types for observability and query results.
 * Re-exports from the main adapter module for backward compatibility.
 * @module @dbsp/types/public/adapter
 */

export type {
	CompiledQuery,
	CompileOptionsBase,
	DumpMeta,
} from '../adapter.js';
